#!/usr/bin/env bash
set -euo pipefail

command -v nvidia-smi >/dev/null 2>&1 || { echo "-"; exit 0; }

# First GPU temp, integer C
nvidia-smi --query-gpu=temperature.gpu --format=csv,noheader,nounits 2>/dev/null \
  | head -n1 | awk '{printf "%d\n", $1}' || echo "-"
