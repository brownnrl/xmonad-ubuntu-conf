#!/usr/bin/env bash

if ! command -v sensors >/dev/null 2>&1; then
  echo "-"
  exit 0
fi

temp=$(sensors 2>/dev/null | awk '/Tctl:/{gsub(/[^0-9.]/,"",$2); print int($2); exit}')

if [ -z "$temp" ]; then
  echo "-"
else
  echo "${temp}°C"
fi
